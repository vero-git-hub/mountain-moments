import React from 'react';

const Header = () => (
    <header>
        <div className="logo">Mountaintop Moments </div>
        <nav>
            {/* Navigation items */}
        </nav>
    </header>
);

export default Header;